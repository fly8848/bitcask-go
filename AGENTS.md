# 仓库指南

## 项目结构

项目介绍（是什么、核心思想、包结构）见 `docs/README.md`。包结构按功能分：`record`（日志记录编解码）、`storage`（数据文件与文件管理）、`engine`（索引与 Put/Get/Delete 主流程）、`merge`（合并压缩）。学习材料与任务清单在 `docs/`；`docs/README.md` 定义阅读顺序（01 材料、02 当前做什么）。

## 构建、测试与开发命令

- `go build ./...` 编译所有包。
- `go test ./...` 运行所有包测试。
- `go vet ./...` 基线静态检查。
- 测试或提交前先 `gofmt -w <changed.go>`。
- 唯一外部依赖是系统 Go 工具链，不引入任何第三方库。

## 编码约定

- 使用 `gofmt` 与惯用 Go 命名；数据布局（磁盘格式）在 `record`，IO 关注点在 `storage`，对外 API 在 `engine`。
- 磁盘上一切数值用固定宽度编码（4B 长度、8B 偏移），字节序自定义但必须一致；跨包格式变更需同步改动测试。
- 对外 API 风格：`New(options)` 返回 `*Engine, error`；`Put`/`Get`/`Delete`/`ListKeys`/`Close`；错误用哨兵变量（`ErrKeyNotFound` 等），不用裸字符串。

## 测试指南

- 每个阶段必须有 `*_test.go`，`go test ./...` 与 `go vet ./...` 全绿才能进入下一阶段。
- 关键测试类型：编解码往返、写读一致性、重启恢复（重建索引）、合并后数据仍完整、随机操作模糊测试。
- 测试用 `t.TempDir()` 建临时数据目录，不留垃圾文件。

## 协作方式

- 用户以学习 Go 与存储引擎为主要目的：默认**引导式推进**——先讲清思路、关键 API 与验证方式，让用户手搓代码；仅在被明确要求时才直接实现。
- 引导式标准流程：先讲"为什么"（设计思路与纠错点）→ 给关键 API 与验证方式 → 搭基础架子 → 用户手搓实现 → review → 测试全绿后再推进。
- 搭架子规范：包、结构体、方法签名、哨兵错误预先立好；方法体留 `panic("TODO")`（保证 `go build`/`go vet` 全绿）；import 不预置，由用户实现时自己补充；测试也要引导用户写，不代劳。
- 引导粒度：注释只给**粗粒度提示**——设计思路、关键不变量、纠错点、自查点；不给分步步骤、具体语句或返回值细节，避免透题。讲解时同样原则：讲"为什么"和"要满足什么"，把"怎么写"留给用户。
  - 允许给：设计思路（为什么这么做）、关键不变量（必须满足的性质）、纠错点（易错处）、自查点（写完怎么验证）、必要时点一下关键 API 名。
  - 禁止给：步骤化指引（"第 1 步做 X"）、具体语句/表达式、变量名、返回值与错误处理的写法。给了这些就变成抄写作业。
  - 正反例（同一方法 Put）：
    - ❌ 细粒度（透题）：`// 第 1 步：record.LogRecord{Key: key, Value: value}` + `// 第 2 步：e.store.Write(&rec)，得到 (fileId, offset)` ...
    - ✅ 粗粒度（引导）：`// 思路：先落盘拿位置，再告诉索引这个 key 的最新位置。` + `// 不变量：索引必须指到磁盘上最新记录，顺序错了重启会读旧值。` + `// 自查：同 key 写两次，Get 返回第二次的值。`
- 高杠杆优先：review 时聚焦影响正确性、数据安全与维护成本的问题（如 CRC 校验缺失、索引不一致、崩溃恢复漏洞、魔法数字）；风格琐碎不影响结果的问题直接忽略。
- 用户明确表示忽略的问题不再重复提出。

## 交付流程

在 `docs/02-task-breakdown.zh-CN.md` 中跟踪进度：一次只推进一个任务，更新状态标记，并在每个里程碑记录实际工时；以任务清单的当前状态为准，不要凭假设。提交使用简洁的 Conventional Commit 风格（如 `feat: add log record encoding`）。绝不提交数据目录、临时文件或真实凭据。
